Понимаю. Подготовлю понятное и подробное техническое задание для агента ИИ Курсора, чтобы он смог реализовать всё на практике. Я аккуратно сформулирую шаги с учётом необходимости загрузки данных из GitHub и соблюдения требований к тестам, меню, корзине, PDF и InStock. Приступаю к подготовке инструкции.


Для реализации проекта оформите задание для Cursor‑агента следующим образом:

1. **Подготовить среду.**

   * Клонировать репозиторий `MarkGerber1/skin-advisor` через подключённый GitHub‑коннектор.
   * Нормализовать каталог: в директории `content/catalog` имеются JSON‑файлы с описаниями (`catalog_descriptions.json`), ссылками (`catalog_links.json`) и ценами/наличием (`catalog_prices.json`). Используйте единую функцию `normalize_key()` для синхронизации ключей между файлами и объединения данных.
   * Обеспечить возможность проверки наличия (`availability_mode="only_in_stock"`) и корректное извлечение цены: сначала `_stock.price`, затем `_price`, затем fallback («Цена на странице магазина»). Для redirect‑ссылок использовать `_ga_url` либо `_ga_query` (поиск).

2. **Сформировать модель данных.**

   * В `engine/models.py` расширить `UserProfile`: добавить поля для тонов волос/бровей/глаз, venous test (теплый/холодный/нейтральный), сезон (`spring`, `summer`, `autumn`, `winter`), а также подробности о коже (тип, обезвоженность, поры, акне, пигментация, морщины, использование ретиноидов, чувствительность и т.д.), как описано в тестах.
   * Реализовать хранение профиля пользователя в БД/kv‑хранилище.

3. **Реализовать FSM‑потоки.**

   * **Палитрометр** (`flow_palette.py`): пошаговый опрос (7–10 вопросов) о волосах, глазах, подтоне, контрасте, форме лица и т.д. По ответам начислять баллы сезонным типам (Весна/Лето/Осень/Зима); при ничьей учитывать undertone и contrast.
   * **Диагностика кожи** (`flow_skincare.py`): опрос для определения типа и состояния кожи (вопросы о стянутости, реакции на солнце, несовершенствах, уходе, аллергиях, желаемом эффекте и т.п.).
   * В каждом потоке блокировать глобальные хендлеры, пока пользователь не завершит тест. После окончания возвращать в главное меню.

4. **Отбор продуктов.**

   * В `engine/selector.py` реализовать правила подбора:

     * Для декоративной косметики — 15 категорий (тон/СС/BB, консилер, корректор, пудра, румяна, бронзер, скульптор, хайлайтер, гель для бровей, карандаш/лайнер/тушь для бровей, тушь для ресниц, тени, каял, помада/блеск/тинт, карандаш для губ). Отбор оттенков основан на определённом сезоне, подтоне, цвете глаз и волос (например, тёплый подтон → персиковые, коралловые, тёплые бежевые тона; холодный — розово‑бежевые, лиловые нюды и т.д.).
     * Для уходовой косметики — 7 категорий (очищение; тонизация/эксфолиация; основной уход (сыворотка/крем); крем для глаз; праймер/база; дополнительный уход (маски, SPF); снятие макияжа). Подбор основан на типе кожи, обезвоженности, пористости, акне, пигментации и чувствительности.
   * Отбирать только товары с `in_stock = true`, заполнять название, краткое описание (`summary_ru`), цену и редирект‑ссылку. По необходимости заменять отсутствующие позиции аналогами.

5. **Интерфейс и меню.**

   * Создать нижнее меню (reply‑keyboard): «🎨 Палитрометр — мой идеальный цвет», «📊 Диагностика кожи PRO», «ⓘ О боте», «🛒 Моя подборка», «⚙️ Настройки».
   * После каждого теста выводить мини‑меню (inline‑клавиатура) с тремя кнопками: «ℹ️ Описание» (подробный портрет), «💆 Рекомендации по нанесению» (пошаговая схема макияжа/ухода) и «🛍️ Что купить?» (список продуктов с кнопками «Купить»).
   * Реализовать боковое меню с пунктами: «⚙️ Настройки» (язык, уведомления, тема, фильтр «только в наличии»), «👤 Профиль» (имя, возраст, пол и краткие итоги тестов), «❓ FAQ», «📞 Контакты».

6. **Корзина и оформление.**

   * При нажатии на «Купить» товар должен добавляться в корзину пользователя. Реализовать экран корзины: список позиций, количество, итоговая сумма.
   * Оформление заказа — переход по редирект‑URL (`/r?url=<encoded>`). Учесть, что в аналитике применяется `user_discount=0.05`, `owner_commission=0.10` (не отображать пользователю).

7. **Генерация PDF‑отчёта.**

   * В `ui/render.py` реализовать формирование PDF на русском языке без ссылок в тексте. Файл должен содержать персональное описание, ключевые рекомендации и 2–3 приоритетных продукта с объяснением «почему подходит»/«как использовать».
   * Использовать fpdf2 или альтернативную библиотеку, соблюдая fallback по шрифтам (`DejaVuSans` → `SegoeUI` → `Arial`).
   * Сохранять отчёт в `data/reports/{user_id}/last.pdf` и записывать метаданные в `last.json`. Пункт меню «📄 Отчёт» должен возвращать последний PDF.

8. **Тестирование.**

   * Написать pytest‑тесты для FSM (корректная нумерация вопросов, запрет перехода без выбора в Q5/Q9, возврат в меню), для подбора (режим only\_in\_stock, корректное заполнение цены, замена OOS), для отображения карточек (заголовок, цена, summary, URL), для корзины (изоляция данных по пользователю, расчёт итогов), для PDF (правильное формирование русского текста и отсутствие гиперссылок).

9. **Документация и проверка.**

   * В README описать, как запустить бота и как подключить каталог.
   * Убедиться, что покрыты все 15 декоративных и 7 уходовых категорий, все пункты меню функционируют, товары загружаются только из каталога и только InStock, PDF формируется корректно.

Следуйте этому заданию шаг за шагом, реализуя код в соответствующих файлах проекта.
