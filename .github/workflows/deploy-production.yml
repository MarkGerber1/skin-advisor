name: Deploy to Railway (Production)

on:
  push:
    branches: [ master ]
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'push' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Railway CLI
        run: |
          npm install -g @railway/cli
          echo "$(npm config get prefix)/bin" >> $GITHUB_PATH

      - name: Verify Railway CLI
        run: |
          railway --version
          echo "🔑 Testing Railway authentication..."
          railway whoami || echo "⚠️  Authentication check failed"

      - name: Deploy to Railway
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          echo "🔍 Checking Railway token..."
          echo "Token length: ${#RAILWAY_TOKEN}"
          echo "Token prefix: ${RAILWAY_TOKEN:0:10}"

          echo "🔍 Checking Railway CLI..."
          railway --version

          echo "🔍 Testing Railway authentication..."
          railway whoami || echo "⚠️  Authentication failed"

          echo "🔍 Checking Railway project status..."
          railway status || echo "⚠️  Status check failed"

          echo "🔍 Listing Railway projects..."
          railway list || echo "⚠️  List failed"

          echo "🔗 Attempting to link Railway project..."
          railway link --yes || echo "⚠️  Link failed"

          echo "🚀 Starting deployment..."
          railway up --detach --verbose

      - name: Notify on success
        if: success()
        run: echo "✅ Production deployment successful!"

      - name: Notify on failure
        if: failure()
        run: echo "❌ Production deployment failed!"
